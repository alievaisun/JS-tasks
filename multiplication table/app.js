let multiplicand = +prompt(`write multiplicand`)
let multiplier = +prompt(` write multiplier`)
let product = 0
for (let product = 0; product>=0; product++) {
    product= multiplicand*multiplier 
    console.log(`product is ${product}`);
}